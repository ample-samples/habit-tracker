export default function Navbar() {
  return (
    <div className="navbar">
      <button>Dashboard</button>
      <button>Today's Habits</button>
      <button>Habit History</button>
    </div>
  )
}
